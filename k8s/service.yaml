apiVersion: v1
kind: Service
metadata:
  name: my-kubernetes-app-service
spec:
  type: NodePort
  selector:
    app: my-kubernetes-app
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 31000
